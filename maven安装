wget http://mirrors.tuna.tsinghua.edu.cn/apache/maven/maven-3/3.5.2/binaries/apache-maven-3.5.2-bin.tar.gz
进到解压后所在目录下，使用  pwd  命令查看全路径名

su

gedit /etc/profile

export M2_HOME=路径全名
export PATH=${M2_HOME}/bin:$PATH

source /etc/profile

mvn -v
